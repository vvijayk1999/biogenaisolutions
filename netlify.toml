[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Redirect the Netlify Identity confirmation links back to the admin panel.
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
